import styled from "styled-components";
import { GlobalBackground } from "../../GloablStyle";

export const Container = styled(GlobalBackground)`
  width: 100vw;
  height: 100vh;

  display: flex;
  justify-content: center;
`;

export const CenterDivWrap = styled.div`
  width: 45%;
  margin-top: 60px;
`;
